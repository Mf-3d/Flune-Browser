import type { Config } from "prettier";

const config: Config = {
  printWidth: 90,
  tabWidth: 2,
  singleQuote: false,
  semi: true,
  trailingComma: "es5",
  singleAttributePerLine: true,
};

export default config;
